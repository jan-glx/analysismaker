---
title: "test"
params:
  example_param_2: "hello world!"
  example_dep_1: "file"
  external_dep_1: "test_external_dep_file.txt"
  results_dir: "results"
---
This is an example notebook. It was knitted with the following parameters:

```{r print-params}
str(params)
```

```{r}
a = as.integer(readLines(params$example_dep_1))
a
```

```{r}
b = readLines(params$external_dep_1)
```

```{r}
writeLines(as.character(a+1), file.path(params$results_dir, "out.txt"))
```
